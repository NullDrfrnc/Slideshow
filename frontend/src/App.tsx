import {Wrapper} from "@/components/Wrapper.tsx";
import {Routing} from "@/components/Routing.tsx";

export const App = () => {
    return (
        <Wrapper>
            <Routing/>
        </Wrapper>
    )
}